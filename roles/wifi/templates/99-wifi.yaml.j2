# {{ ansible_managed }}

network:
    wifis:
        wlan0:
            access-points:
                "{{ wifi_network_name }}":
                    password: "{{ wifi_password }}"
            dhcp4: true
            optional: true
    version: 2
